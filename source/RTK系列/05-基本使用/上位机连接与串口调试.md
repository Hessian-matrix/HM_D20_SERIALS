# 上位机连接与串口调试

本页说明D20、D13的通用UART连接、NavStarTool状态查看和Linux串口检查。接线前先确认[D20接口](D20接口与接线.md)或[D13接口](D13接口与接线.md)。

## 串口参数

| 项目 | 默认值 |
| --- | --- |
| 波特率 | 115200 bps |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验 | None |
| 流控 | None |

D20、D13 外部 UART IO 电平为 3.3 V。USB 转 UART 模块或目标设备必须兼容 3.3 V IO 电平，且不能使用 RS232 电气接口。

## 连接NavStarTool

1. 完成供电、TX/RX交叉连接和共地。
2. 打开NavStarTool，选择设备对应串口。
3. 波特率选择`115200`，点击连接。
4. 确认卫星数、经纬高、轨迹和原始数据持续刷新。

![NavStarTool主界面](image/navstar_main.png)

NavStarTool 的星空图用于观察卫星分布，CN0 窗口用于查看信号强度，轨迹图用于查看定位点变化，NMEA 信息窗口用于检查D20地面版和D13输出。D20无人机版UBX输出应通过目标飞控及其地面站验证。

## 串口控制台

可通过`Receiver -> Console`打开控制台。没有持续数据时，依次检查：

1. 串口号和波特率。
2. TX/RX是否交叉连接。
3. 设备和调试器是否共地。
4. 供电电压和极性。
5. 当前固件是否输出预期协议。

## Linux下确认串口

连接USB转UART模块后，可使用以下命令确认设备节点：

```bash
dmesg --follow
ls -l /dev/ttyUSB*
```

例如设备节点为`/dev/ttyUSB0`时：

```bash
picocom -b 115200 /dev/ttyUSB0
```

出现持续数据表示串口链路已建立；协议含义和刷新率由出厂固件决定。

## 下一步

- 输出协议不符合目标设备要求：查看[固件与输出协议选择](固件与输出协议选择.md)。
- 已有数据但未进入固定解：查看[RTK状态与固定解验证](RTK状态与Fixed验证.md)。
- 串口仍无法连接：进入[故障排查](../09-常见问题/01-故障排查.md)。
